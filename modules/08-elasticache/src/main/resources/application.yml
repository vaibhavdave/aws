server:
  port: 8088

# elasticache.host / elasticache.port are deliberately absent - TaskCacheApplication
# provisions the ElastiCache cluster first and sets them as system properties, since the
# endpoint isn't known until the Docker-backed cluster is available (same reasoning as
# Module 07's spring.datasource.*).
